import { SheetContent, SheetHeader, SheetTitle,  } from "../ui/sheet";

export default function SourcePart() {
    return (
      <SheetContent>
        <SheetHeader>
          <SheetTitle>
            勝利とは、最後まで屈しないことである。
          </SheetTitle>
        </SheetHeader>
        <div>
          contents here.
        </div>
      </SheetContent>
  )
}